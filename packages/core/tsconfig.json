{
  "extends": "../../config/tsconfig",
  "compilerOptions": {
    "declarationDir": "./dist/types",
    "outDir": "./dist",
    "baseUrl": "./src",
    "downlevelIteration": true // Provide full support for iterables in 'for-of', spread, and destructuring when targeting 'ES5' or 'ES3'. Requires TypeScript version 2.3 or later
  }
}
